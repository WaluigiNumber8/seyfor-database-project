<UserControl x:Class="SeyforDatabaseProject.Views.Guests.GuestsEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SeyforDatabaseProject.Views.Guests"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="8" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
        </Grid.RowDefinitions>
        <Label Grid.Row="1"
               Content="{Binding HeaderText, FallbackValue=Add Guest}"
               HorizontalAlignment="Center"
               FontSize="18"
               FontWeight="Medium" />
        <Separator Grid.Row="2" />
        <ScrollViewer Grid.Row="3">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Stretch">

                <Label Content="Name" />
                <TextBox Text="{Binding PickableItems, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Width="200"
                         Height="30"
                         Margin="0,0,0,8"
                         HorizontalAlignment="Center" />
                <Label Content="Surname" />
                <TextBox Text="{Binding Surname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Width="200"
                         Height="30"
                         Margin="0,0,0,8"
                         HorizontalAlignment="Center" />
                <Label Content="Email" />
                <TextBox Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Width="200"
                         Height="30"
                         Margin="0,0,0,8"
                         HorizontalAlignment="Center" />
                <Label Content="Phone Number" />
                <TextBox Text="{Binding PhoneNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Width="200"
                         Height="30"
                         Margin="0,0,0,8"
                         HorizontalAlignment="Center" />


            </StackPanel>
        </ScrollViewer>
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,16,0,16">

            <Button Content="Remove"
                    Command="{Binding RemoveCommand}"
                    Visibility="{Binding ShowRemoveButton, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Width="100"
                    Height="30"
                    Margin="0,0,64,0" />
            <Button Content="{Binding SaveButtonText, FallbackValue=Save}"
                    Command="{Binding SaveCommand}"
                    Width="100"
                    Height="30"
                    Margin="0,0,8,0" />
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Width="100"
                    Height="30" />
        </StackPanel>
    </Grid>
</UserControl>